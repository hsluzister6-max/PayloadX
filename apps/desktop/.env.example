# Optional local-dev hints only.
# Runtime URLs come from the login server picker:
#   Cloud Build → https://payload-x-884697093779.europe-west1.run.app
#   Local Setup → URL entered by the user
# VITE_API_URL / VITE_SOCKET_URL are not used for cloud/local routing.

# Optional: enable AST CLI route sync (omit to avoid ws://localhost:4040 noise)
# VITE_AST_CLI_WS=ws://localhost:4040
