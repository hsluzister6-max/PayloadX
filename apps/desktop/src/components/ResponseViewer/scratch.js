const fs = require('fs');

function formatJsonToLines(obj, path = '', depth = 0, collapsedPaths, isLast = true) {
  const lines = [];
  const currentPath = path || 'root';
  // ... (copy from the file context) ...
  // just minimal logic to test timing
}
